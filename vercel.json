{
    "github": {
        "silent": false,
        "deploymentProtections": true
    },
    "builds": [
        {
            "src": "package.json",
            "use": "@vercel/next"
        }
    ],
    "ignoreCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" = \"main\" ] || [ \"$VERCEL_GIT_COMMIT_REF\" = \"staging\" ]; then exit 1; else exit 0; fi"
}